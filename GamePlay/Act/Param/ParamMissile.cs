using UnityEngine;
using System.Collections;
using System;

namespace Cfg.Act
{
    public class ParamMissile : ParamSpawnObj
    {
        public override void Read(string[] array)
        {
            base.Read(array);
        }

        public override void Save(ref string s)
        {
            base.Save(ref s);
        }
    }
}
using UnityEngine;
using System.Collections;

namespace NLE.Core
{
    public enum CutsceneState
    {
        Inactive,
        Playing,
        PreviewPlaying,
        Scrubbing,
        Paused
    }
}